# UVT: улучшения для Apple M4 / 16 ГБ

Действующий Git-проект: `/Users/teotim/Documents/голос`, 2026-09-06. Пользователь окончательно выбрал эту папку. Перед продолжением обе копии сравнены: основные исходники уже совпадали, все последующие исправления и проверки выполнены в `голос`. Модели, Python-окружение, секреты и Git-ветка сохранены.

- [x] Простой запуск: `README_START.md`, ссылка в основном README и `Запустить UVT.command` для всех трёх режимов с открытием панели. Обёртка `scripts/serve-personal.sh` передаёт параметры запуска.

## Bunkr: Unsupported URL — 2026-09-06

- [x] Подтверждена причина: публичная страница bunkr.cr/f/... создаёт плеер после получения временного token/ex, обычных HTML5 source и Playerjs file в исходном HTML нет.
- [x] Добавлен разбор отдельного блока строковых jsCDN/jsType/signUrl и штатный публичный запрос подписи. JS сайта не исполняется, cookie/авторизация не передаются; signer закреплён, ответы/время ограничены. Комментарии, regex, выражения, вложенный код и повреждённые блоки не считаются конфигурацией (src/uvt/media_discovery.py).
- [x] Подписанная ссылка передаётся существующему извлечению звука через FFmpeg после Unsupported URL. Ошибки публичного плеера показываются отдельно; временная query-подпись исключена из сообщения загрузки в журнале (src/uvt/server.py).
- [x] 100 pytest за 0,77 с: 50 новых Bunkr-сценариев и 50 существующих проверок HTML5/Playerjs/загрузки/прогресса (tests/test_bunkr_discovery.py, tests/test_media_discovery.py, tests/test_source_download.py, tests/test_source_progress.py). Проверены передача подписанного потока загрузчику, query-параметры, HTTP-отказы, лимиты, отмена и отсутствие cookie.
- [x] Финальная функция на пользовательской странице нашла один поток; HEAD потока вернул HTTP 200 и Accept-Ranges: bytes. Полный видеофайл и модели в этой проверке не загружались.
- [ ] Применение: перезапустить текущий UVT после окончания активного задания и повторить перевод. Полный дубляж данного видео ещё не проверен. Userscript для этого серверного исправления менять не требуется.
- Откат: сохранить последующие правки; вернуть изменения Bunkr в src/uvt/media_discovery.py и добавленную диагностику src/uvt/server.py, сохранив предыдущие исправления выбора голосов.

## HTTP 422 при смене профиля/голоса — 2026-09-06

- [x] Userscript 0.18.5 отправляет автоматический локальный голос строкой, без null. Облачным маршрутам не передаются локальные voice_id/profile_id (browser/uvt.user.js).
- [x] Выбор профиля/языка явно запрашивает settings_mode=override. Сервер возвращает каталог выбранного профиля даже при сохранённых глобальных настройках, сохраняя явный settings_mode=server (src/uvt/server.py).
- [x] При смене профиля старый список голосов сразу скрывается; ошибки и запоздалые ответы не возвращают его. Совместимые голоса сохраняются, несовместимые сбрасываются; последующее обновление не восстанавливает несовместимый голос из глобальных настроек.
- [x] Старое voice_id:null поддержано только на границе запроса перевода/пробы. Глобальные настройки и некорректные типы по-прежнему проверяются строго.
- [x] 38 серверных pytest: 17 новых регрессионных случаев плюс существующие проверки профилей/пар голосов. Проверены запросы /dub, /meta и /tts/preview с тестовыми движками (tests/test_server_legacy_null_voice.py, tests/test_voice_pair_server.py, tests/test_next_model_settings.py).
- [x] 2 pytest-обёртки / 13 нейтральных сценариев Chrome: глобальный MOSS → выбранный Nemotron/Piper, зависшие/ошибочные/запоздалые каталоги, совместимость голоса, языки, отправка перевода и предпрослушивание (tests/userscript_voice_profiles.cjs, tests/userscript_video_settings.cjs). node --check и git diff --check пройдены.
- [ ] Применение в браузере пользователя: обновить существующую копию до 0.18.5 и перезагрузить вкладку, включая инкогнито. Применение серверного кода: штатный перезапуск после окончания текущего перевода. Работа на реальной странице пользователя после обновления ещё не подтверждена.
- Откат: сохранить последующие пользовательские правки, вернуть прежние версии browser/uvt.user.js и src/uvt/server.py; в Tampermonkey установить ту же версию скрипта, затем перезагрузить вкладку.

## MOSS: предел реплики и пропуски озвучки — 2026-09-06

- [x] Диагностика: 375 кадров кодека соответствуют 30 с звука; достижение лимита не подтверждает завершение реплики. Старый обработчик логировал ошибку и пропускал реплику.
- [x] Ограниченное восстановление с тем же голосом/образцом: адаптивный предел, проверка завершения дополнительным кадром, разделение только проблемного текста, один повтор для неделимой части, общий предел попыток (src/uvt/engines/tts_moss_onnx.py, profiles/local-moss.yaml).
- [x] Исчерпанное восстановление останавливает текущий дубляж с понятной причиной; следующие задания и другие движки не блокируются (src/uvt/dub.py).
- [x] 39 focused pytest: MOSS, фатальная ошибка дубляжа и существующие проверки скорости. Проверены сохранность текста/порядка/голоса, отсутствие декодирования незавершённых кадров, восстановление лимита после ошибки/отмены, предел повторов, завершение активных операций и новая задача после ошибки (tests/test_tts_moss_onnx.py, tests/test_dub_moss_failure.py, tests/test_dub_speed.py).
- [x] Дополнительно 21 pytest подтвердил сохранение поведения ElevenLabs и чередования голосов (tests/test_tts_elevenlabs.py, tests/test_dub_voice_pairs.py). Всего по двум исправлениям прошли 100 pytest-проверок в профильных наборах; две из них запускают 13 браузерных сценариев.
- [!] Реальная проверка нового восстановления на нейтральном аудио пока не выполнена: перед загрузкой модели обнаружена новая активная пользовательская задача, запуск остановлен без загрузки весов. Предыдущие измерения MOSS не подтверждают скорость этого исправления.
- [ ] Применение: штатно перезапустить UVT после завершения текущего задания и повторить проблемный перевод. Длинный дубляж с конкретными образцами пользователя ещё не проверен.
- Откат: сохранить текущие правки и вернуть только перечисленные исходники/профиль из предыдущей версии; пользовательские глобальные настройки и модели не менялись.

## Failed to fetch между сайтом и работающим UVT — 2026-09-06

- [x] Пользователь подтвердил причину старой ошибки связи: в инкогнито выполнялась не обновлённая копия userscript.
- [x] Живые GET `/meta` всех трёх маршрутов вернули 200 и CORS `*`. Free сообщает семь установленных профилей. Старые «· проверка» отсутствуют в 0.18.3 и указывают на не обновлённую копию userscript; установленная копия отдельно не прочитана.
- [x] Userscript 0.18.4: сетевой API Tampermonkey, разрешения только `127.0.0.1`/`localhost`, без cookie, с отменой и собственным таймаутом. Сохранён `X-UVT-Token`, бинарные ответы и серверные HTTP-ошибки; POST не повторяется автоматически. CORS/авторизация сервера не ослаблялись (`browser/uvt.user.js`).
- [x] Версия видна в заголовке; способ связи и адрес — в диагностике. Сетевая ошибка объясняет подключение вместо сообщения про DRM/модели. При неизвестной готовности выбор профиля доступен.
- [x] Готовая сжатая дорожка загружается через тот же транспорт и Blob URL; прогрессивная озвучка сохраняется до готовности, отменённая загрузка не подключает звук, URL освобождается. 10 проверок жизненного цикла прошли (`tests/userscript_completed_audio.cjs`). Строгий `media-src`, запрещающий Blob, остаётся ограничением сайта.
- [x] Итог: **4 pytest-проверки прошли за 41,01 с**: 24 сценария плашки, 6 настроек, 15 транспорта и 10 жизненного цикла звука. Транспорт проверен в Chrome под CSP `connect-src none` с имитацией classic/modern GM; дополнительный read-only мост получил реальные `/meta` рабочего UVT. POST `/dub` имитировался, модели/квота не запускались. Реальный установленный Tampermonkey этим не проверяется. JavaScript syntax и `git diff --check` прошли (`tests/userscript_transport.cjs`, `tests/test_userscript_transport.py`, `tests/test_userscript_completed_audio.py`).
- [x] По дополнительному запросу проверены процессы: один сервер UVT, PID 19431, cwd `/Users/teotim/Documents/голос`, порты 8765–8767; Free использует сохранённый `local-moss`. Сервер отдаёт 0.18.4. Это снимок процесса на момент проверки, не перечень установленных расширений.
- [!] Управление расширениями Chrome закрыто политикой инструмента: переход на `chrome://extensions/` отклонён, обход не выполнялся. Пользователь должен обновить существующий скрипт по локальной ссылке и подтвердить его сетевые разрешения. Рабочие серверы и модели для исправления не перезапускаются.

## Возврат плашки и настройки отдельного видео — 2026-09-06

- [x] По уточнённому запросу убран строгий фильтр основного плеера/превью: плашка снова доступна на всех видимых видео, в том числе во вложенных плеерах и списках. Наложенные друг на друга video не дублируют кнопки (`browser/uvt.user.js`, 0.18.3).
- [x] После бездействия мыши плашка полностью прозрачна и не перехватывает клики. Движение над плеером возвращает управление; неподвижная мышь над кнопкой не удерживает плашку. Открытые настройки, ошибки и клавиатурный фокус остаются доступными.
- [x] Профиль, язык и голос конкретного видео наследуют глобальные значения и меняются отдельно. Шесть браузерных сценариев проверяют независимость двух видео, запросы перевода, сброс, выбор без сервера, Free/GPT и отказ для отсутствующих моделей без подмены профиля (`tests/userscript_video_settings.cjs`, `tests/test_userscript_video_settings.py`).
- [x] Проверены 24 нейтральных сценария в Chrome: вложенность, сетки, YouTube-подобная страница, несколько видео, iframe, SPA, fullscreen, полная прозрачность/попадание мыши/возврат, клавиатура, экран 390 px. Ошибок браузера нет. Тесты: `tests/userscript_main_player.cjs`, `tests/test_userscript_main_player.py`.
- [x] Итоговый совместный запуск: **2 pytest-проверки прошли за 39,10 с** (24 + 6 сценариев Chrome); прежние шесть сценариев настроек также прошли, включая отмену, defaults и 390 px. JavaScript syntax и `git diff --check` чистые. Тестовые браузеры закрыты; UVT уже отдаёт 0.18.3 из текущего проекта.
- [!] Это проверка реального DOM на нейтральных страницах; установка расширения и воспроизведение на внешних сайтах в этой проверке не выполнялись. Рабочий UVT не перезапускается; для активации обновить существующий userscript в Tampermonkey и перезагрузить вкладку. Для отката только этого изменения сохранить последующие правки и восстановить перечисленные файлы из предыдущей Git-версии.

## Глобальные голоса и устойчивость говорящих — 2026-09-06

- [x] Выбор мужского/женского голоса хранится отдельно для движка и языка; явный единый голос остаётся приоритетным (`config.py`, `server_settings.py`, `server.py`, TTS-движки и `services/tts.py`). Для каждой роли есть отдельная проба; облачные пробы используют квоту провайдера.
- [x] F5/MOSS используют сохранённые локальные образцы вместо исходного клонирования, если образец выбран. Загрузка 3–30 с / до 20 МБ, точный текст, библиотека до 100 записей; авторизация, границы путей и приватные права файлов проверены (`voice_references.py`, `/voices/reference`, `tests/test_voice_references.py`).
- [x] На отдельном проверочном сервере браузером проверены сохранение пар Piper/MOSS/GPT/ElevenLabs, выбор роли для пробы, переключение языка и сброс; загрузка F5-образца вернула 201, сохранение — 200. Это проверка интерфейса/API, не подтверждение качества каждого голоса на слух.
- [x] Устойчивость спикера: F0 берётся по пику, спектральные признаки — по озвученным кадрам, короткие фразы наследуют ближайшего говорящего по времени; два голоса одного диапазона не получают принудительно противоположные роли (`gender.py`, `diarize.py`, `services/speaker.py`). 59 профильных проверок прошли, включая ручные настройки и live.
- [x] Нейтральные 13,97 с / пять фраз: до исправления 2 спикера, после 1 с двумя вариантами таймингов. Пограничный тембр использует устойчивый fallback; пол человека не считается установленным. Доказательство: `artifacts/verification/voice-selection/speaker-stability.json`.
- [x] Предыдущий этап, Userscript 0.18.2: управление только на основном плеере (фильтр отменён по запросу в 0.18.3); 29 нейтральных DOM-сценариев Chrome (`browser/uvt.user.js`, `tests/userscript_main_player.cjs`).
- [x] Короткая инструкция и ограничения описаны в `README_START.md` и `docs/VOICE_SELECTION.md`.
- [x] Дополнительные локальные голоса: в MOSS открыты Adam/Nathan и Ava/Bella; можно добавлять свои образцы в MOSS/F5. Альтернативный Silero из стороннего зеркала не подключался: выполнение пакета отклонено автоматической проверкой разрешений.
- [x] Контекст файлового перевода передаёт до трёх соседних реплик с каждой стороны по умолчанию (настройка `translation.file_context_lines`, 0–6; всего до 2400 символов). Реализовано для Hy-MT2, MLX-chat, OpenAI и совместимого fallback; история служит контекстом без повторного перевода. Live использует только прошлые реплики; NLLB/TranslateGemma остаются на прежнем контракте (`dub.py`, переводчики, `tests/test_file_translation_context.py`). Перенос эмоции оригинала не реализован; оценка качества на диалогах остаётся отдельной проверкой.
- [x] Итоговая объединённая проверка: 651 тест прошёл за 20,88 с; JS syntax и diff-check чистые. В браузере сохранены пары Free/GPT/ElevenLabs, новый голос F5, проверен экран 390 px без переполнения. MOSS/F5 чередуют роли в одном экземпляре с корректной отменой (7 тестов). Временные порты закрыты; пользовательский UVT требует обычного перезапуска. Артефакты: `artifacts/verification/voice-selection/README.md`.
- [ ] Реальные длинные диалоги: смешанные/похожие голоса, музыка, шёпот и эмоциональная речь. Отдельно оценить натуральность интонации и клонирования на слух.

Активация: пользовательский рабочий сервер этого этапа не перезапускается автоматически. После завершения своих задач остановить UVT через Ctrl+C, запустить обновлённый проект и обновить userscript в Tampermonkey. Возврат к прежнему поведению — автоматические голоса движка или «Из оригинала / автоматически» для образцов; настройка одного голоса остаётся доступной. Старые результаты сохраняются, переозвучка выполняется только новой задачей.

## Повторный запуск и занятые порты — 2026-09-06

- [x] Остановлен оставленный после проверки экземпляр UVT; активных переводов не было, готовые результаты сохранены.
- [x] `src/uvt/server.py` / `src/uvt/cli.py`: занятый порт сообщает адрес без traceback; прогрев моделей начинается после открытия порта.
- [x] Проверки: 6 новых тестов занятого порта/CLI; общий набор — 525 passed. Временный сокет закрыт, рабочие порты свободны.
- Активация: `uvt serve-personal`. Откат: отменить только обработку `ServerBindError`.

## Интеграция выбранных моделей и просмотр видео — 2026-09-06

- [x] Итог после объединения: 519 тестов прошли, JS syntax и diff-check чистые. Три маршрута перезапущены, профили установлены. Старое видео восстановлено без моделей; исходник и аудио побайтово сохранены, MP4 и Range 206 проверены (`artifacts/verification/next-local-models/integration-final.json`).

- [x] Hy-MT2 1.8B 4-bit: отдельный движок с native user-only prompt, RU/UK, ограничением генерации и контекстом без повторного перевода истории (`src/uvt/engines/translate_hymt_mlx.py`, `profiles/local-hymt.yaml`). 14 профильных тестов; реальные веса и EN→RU/UK проверены офлайн.
- [x] MOSS ONNX на CPU: встроенные Adam/Bella и синтез с референсом, без зависимости от TorchCodec (`src/uvt/engines/tts_moss_onnx.py`, `profiles/local-moss.yaml`). Русская озвучка проверена: CPU RTF 0,70/0,82. Украинский язык не поддерживается выбранной 19-язычной ONNX-моделью.
- [x] Nemotron 3.5 8-bit: адаптер готового аудио, таймкоды последнего накопительного результата, отмена после текущего чанка и обязательное освобождение модели (`src/uvt/engines/stt_nemotron_mlx.py`, `profiles/local-nemotron.yaml`). 29 профильных тестов; 13,97 с английской речи распознаны за 1,59 с.
- [x] Новые профили доступны для выбора; веса скачаны в `.models/` текущего проекта. `setup-mac-local` поддерживает `hymt`, `moss`, `nemotron`, закреплённые ревизии и локальную проверку (`src/uvt/setup_local.py`, `pyproject.toml`). 32 проверки установки: ошибка ревизии/метаданных, пустые или отсутствующие файлы, отсутствие сетевого обращения для моделей внутри проекта, MOSS без Piper.
- [x] ElevenLabs: подтверждён настоящий `payment_required` для старого автоматического женского Voice Library ID на Free. Auto теперь Sarah/Adam, оба дали HTTP 200 на текущем ключе; явный выбор голоса не подменяется. Сохранена точная безопасная причина ошибки вместо предположения о пустом балансе (`src/uvt/engines/tts_elevenlabs.py`, `src/uvt/dub.py`, `tests/test_tts_elevenlabs.py`).
- [x] Готовый перевод можно смотреть в студии с отдельной громкостью оригинала и перевода; подготовка исходного видео и сохранение нового микса не повторяют распознавание/перевод. Новые/сохранённые задания и ограничения описаны в `docs/VIDEO_RESULTS.md`; проверка плеера выполнена на нейтральном тестовом видео.
- [x] Обновлены `README_START.md`, `docs/LOCAL_MODELS_M4.md`, `docs/LIVE_DIALOGUE_M4.md`: запуск, настройки, измеренные показатели и ограничения. Доказательства отдельных моделей: `artifacts/verification/next-local-models/`.
- [x] Полные `local-moss` и `local-nemotron` завершили офлайн EN→RU на одном нейтральном файле 13,97 с: **36,1 с / 15,5 с** с загрузкой моделей и укладкой речи; созданы WAV/SRT/JSON. Это одиночные короткие проверки, не оценка средней скорости и не тест звонка (`artifacts/verification/next-local-models/README.md`).
- [x] DoD всего этапа: 519 тестов прошли после объединения; каталог моделей проверен на рабочем сервере, видеоплеер — в браузере на нейтральном ролике, старый результат — через HTTP и проверку MP4. Итог: `artifacts/verification/next-local-models/integration-final.json`.
- [ ] Сравнить полные новые профили на одинаковом нейтральном видео: текст, грамматика, тайминг, время, пропуски и общий пик памяти. Есть ошибки Hy-MT2 и английского ASR Nemotron; победитель по качеству пока не установлен.
- [ ] Проверить RU/UK распознавание Nemotron и длинную русскую речь MOSS; оценить качество референсного голоса отдельно от факта создания WAV.
- [ ] Непрерывный звонок: PCM между фразами, сохранённое состояние STT, стабильные partial/final, контролируемая очередь/эхо. Текущий desktop Live работает с завершёнными фразами; URL/userscript готовят видео пакетно.
- [ ] Аппаратный тест звонка 10–15 минут: медиана/p95 задержки, первый звук, очередь, память и потери. Отдельный быстрый smoke движка не заменяет этот тест.

Активация правок: сервер штатно перезапущен 2026-09-06, все три маршрута работают. Для загруженных ранее страниц обновите вкладку; если установлен старый userscript, обновите его в Tampermonkey. Возврат: выбрать `local-balanced`/`local-dialogue` в настройках; веса и явные пользовательские настройки не требуется удалять.

## Исправление F5 / TorchCodec — 2026-09-06

- [x] Подтверждена причина: FFmpeg 8 требует существующую `libx265.216.dylib`, но изменение окружения уже работающего Python не помогает нативному TorchCodec. Один и тот же WAV не читался после позднего `ensure_media_runtime`, но читался с окружением до старта Python.
- [x] `scripts/serve-personal.sh`, `src/uvt/media_runtime.py`, `src/uvt/cli.py`: файл запуска использует проверенную обёртку; прямой CLI при необходимости перезапускает себя с сохранением аргументов и окружения до создания серверов/моделей.
- [x] `src/uvt/engines/tts_f5.py`, `src/uvt/dub.py`: проверка настоящего декодера F5 до обработки ролика и загрузки модели; краткая ошибка вместо повторения трассировки для каждой реплики. Успех проверки кэшируется, ошибку можно проверить повторно.
- [x] Исправлен дополнительный дефект F5: случайный seed ограничен диапазоном Python `0..4294967295`, явный seed проверяется. Это устраняет ошибку старта дочерних Python-процессов.
- [x] Проверка: 380 тестов прошли; настоящий F5 Russian / MPS / 16 шагов создал 5.013 с нейтральной русской речи полностью офлайн. Повторный синтез: 42.301 с; загрузка модели: 11.750 с. Журнал и воспроизводимая проверка: `artifacts/verification/f5-native-runtime/`.
- [x] Активация: остановить старый процесс Ctrl+C и открыть `Запустить UVT.command`; прямой `uvt serve-personal` также исправлен. Тестовые серверы не запускались. Откат: отменить только этот diff и перезапустить UVT.
- [ ] Полный ролик и качество F5 на слух в этой проверке не оценивались. `local-natural` остаётся медленным; для M4 / 16 ГБ рекомендация по скорости — `local-balanced`.

## Живой диалог и новое исследование моделей — 2026-09-06

- [x] Разобран пользовательский лог: decode/Demucs 205,4 с; STT 41,7 с; MT/сокращение 545,6 с; окончание F5 в присланном логе отсутствует. Ошибки смысла подтверждены, происхождение конкретных ошибок до/после сокращения не доказано.
- [x] Исследование HF/GitHub/PyPI: `docs/LIVE_DIALOGUE_M4.md`. Новые приоритеты — Hy-MT2 1.8B MLX для MT, MOSS Nano ONNX для RU TTS, Nemotron 3.5 для будущего streaming ASR. Civitai API вернул 403; каталог не объявлен полностью изученным.
- [x] `translate_mlx_chat.py`: убран повторный перевод истории в live, сохранён контекст текущего запроса; проверен `context_pairs=0`.
- [x] `services/base.py`, `app.py`, `gui/main_window.py`: модели готовы до начала захвата, последовательный прогрев, штатная остановка/ошибка на этапе подготовки и освобождение ресурсов после текущей загрузки.
- [x] Добавлены `profiles/local-dialogue.yaml` и `Запустить диалог.command`, профили видны в GUI/CLI. Короткие фразы 320 мс / до 4 с, Parakeet → NLLB → Piper без Demucs/F5; офлайн-проверка существующих моделей успешна.
- [x] DoD текущего этапа: 392 теста прошли, проверен Qt-интерфейс без звуковых устройств/моделей, синтаксис launcher, diff. Доказательства: `artifacts/verification/live-dialogue-m4/`.
- [ ] Аппаратный замер живого EN→RU/UK диалога: медиана/p95 после конца фразы, первый звук, память и пропуски. Во время разработки пользовательская F5-задача ещё работала; тяжёлые замеры параллельно не запускались.
- [x] Hy-MT2: после первоначального исследования веса скачаны и движок интегрирован; реальные EN→RU/UK измерения приведены в новом разделе выше. A/B с TranslateGemma/Qwen на одинаковом корпусе остаётся дальнейшей проверкой.
- [x] MOSS ONNX: добавлен CPU-адаптер и получен реальный русский звук; подробности и измерения выше. Длинная речь и качество клонирования ещё требуют отдельной оценки.
- [ ] Браузерный поток аудио и два направления звонка: отдельный следующий этап. Chrome/URL перевод пока пакетный; новый профиль обслуживает настольный live-вход.
- [x] Активация/откат описаны в README: новый запуск после текущей тяжёлой задачи; откат только live-diff, предыдущие исправления F5 сохранить. Текущий сервер/задача не прерывались; новых проектов, коммитов и публикаций нет.

## Критерий готовности

Из браузера можно отправить скачанное видео, наблюдать этапы, отменить обработку и скачать дорожку, субтитры и видео. Userscript совместим с API. Локальный профиль не отправляет текст/аудио провайдерам. Проверки поведения отделены от измерений реальных моделей.

- [x] Исправить выбор источника: текущий плеер имеет приоритет над рекламными сетевыми ресурсами; HTML5/Playerjs fallback после Unsupported URL (`src/uvt/media_discovery.py`, `src/uvt/server.py`, `browser/uvt.user.js`). На первой проблемной странице найдены четыре MP4, трёхсекундное извлечение аудио прошло; на второй MP4 из пользовательского лога тоже декодирован, хотя HTML страницы ответил 503. Полный перевод этих видео не проверялся.
- [x] Исправить запуск FFmpeg при установленной несовместимой x265 ABI: выбор существующей совместимой библиотеки только для процесса UVT (`src/uvt/media_runtime.py`, `src/uvt/cli.py`). Системные библиотеки и ссылки не изменялись.
- [x] Изучить архитектуру, профили, зависимости и похожие проекты; сравнить модели и RU/UK голоса по первоисточникам (`docs/LOCAL_MODELS_M4.md`).
- [x] Уменьшить лишние выделения памяти при дубляже на ~1,38 ГБ для часовой дорожки; смешивать массивы на месте. Перенести блокирующие I/O и экспорт из event loop; при отмене дождаться завершения уже запущенного worker (`src/uvt/dub.py`, `src/uvt/server.py`). Это уменьшение выделений, не замер такого же снижения пика памяти всей модели.
- [x] Устранить накопление очередей при перезапуске Live (`src/uvt/services/base.py`).
- [x] Обновить userscript до 0.18.1: читаемые настройки, мобильный и полноэкранный режим, корректные defaults/overrides, ранние URL медиапотока, прогресс готового звука, остановка звука при отмене, переход к файловой студии (`browser/uvt.user.js`).
- [x] Добавить `/workspace`: файл/ссылка, drag-and-drop, языки/профиль/голос, этапы, отмена, восстановление и история, скачивание M4A/SRT/VTT/TXT/JSON и MKV с двумя дорожками (`src/uvt/server_workspace.py`, `src/uvt/web/`, `src/uvt/server.py`).
- [x] Ограничить загрузку 2 ГБ/90 минутами и очередь восемью заданиями; проверять контейнер и аудиопоток до запуска моделей; удалять временный исходник после окончания/отмены. Проверены токены результатов, некорректный multipart, таймауты, отмена до запуска coroutine и безопасные имена файлов.
- [x] Добавить экспериментальный `local-meeting` для локальных субтитров звонка: Parakeet → TranslateGemma без TTS. Требуется явная настройка BlackHole/аудиовхода; встроенной WebRTC-интеграции пока нет.
- [x] Измерить установленный `local-balanced` на нейтральной EN→RU записи с отключённой сетью моделей (`artifacts/benchmark/BENCHMARK.md`). 13,97 с аудио: первый вызов 38,92 с, повторный 20,13 с, первый клип 37,39/19,06 с; пик memory footprint процесса 4,90 GiB. Голоса новых моделей и длительный звонок не тестировались.
- [x] Проверить Python API/конвейер, JavaScript, desktop/mobile UI и настоящий upload→export с тестовыми движками. Подробности ниже.

## Исправление local-natural после лога 13:53

- [x] Подтверждено: исходник уже скачан, задача `741946eea97b` отменена через 160,1 с; основной серверный профиль остаётся `local-balanced`, для этой задачи был выбран `local-natural`.
- [x] Отдельные этапы декодирования и отделения речи, прогресс по нативным фрагментам Demucs, монотонный общий индикатор (`src/uvt/dub.py`, `src/uvt/server.py`).
- [x] Отмена Demucs между фрагментами с ожиданием worker и освобождением GPU; подготовленный STT освобождается до запуска Demucs и загружается после него (`src/uvt/separate.py`, `src/uvt/server.py`).
- [x] Demucs по умолчанию использует только локальный кэш. YAML и нужные 84 МБ весов уже найдены на этом Mac без загрузки самой модели. Поддержан прежний формат torch.hub checkpoint.
- [x] Userscript 0.18.1 и файловая студия показывают стоимость тяжёлого профиля и рекомендуют balanced для скорости. Проверены новые этапы, возврат к настройкам сервера и экран 390 px.
- [x] Общий набор: **337 passed in 9.53s**; после уточнения округления прогресса повторно прошли 25 профильных проверок. Отчёт: `artifacts/verification/natural-profile.json`, лог `natural-profile-tests.txt`, снимки `natural-profile-*.png`.
- [!] Реальная скорость Demucs после изменений не измерялась. Работающий пользовательский сервер и его настройки не изменялись: для применения кода нужен обычный перезапуск UVT и обновление userscript в Tampermonkey.

## Исправление загрузки ссылки из интерфейса после лога 14:16

- [x] yt-dlp получает `--newline` и явный прогресс; общий процессный reader обрабатывает CR/LF и длинные строки, не блокируя загрузчик (`src/uvt/server.py`).
- [x] Передача страницы, проверка медиапотоков и загрузка с неизвестным размером отображаются отдельно. Ограничены сокет и число повторов; получение файла должно начаться за 120 с, простой передачи ограничен 90 с, общий бюджет здоровой загрузки — 30 минут (`src/uvt/source_download.py`). Для HLS учитываются реальные обновления времени FFmpeg.
- [x] Тайм-аут охватывает процесс и чтение вывода; повторная отмена дожидается очистки собственных дочерних процессов. Ошибка сохраняет `failed_stage`.
- [x] В студии до подтверждённого прогресса показываются ожидание и время, без выдуманных процентов. При ошибке источника доступны Chrome и локальный файл; тяжёлый профиль не выдаётся за причину ожидания загрузки.
- [x] Реальный загрузчик получил первые 3 с аудио по проблемной ссылке: 4,04 с обработки, выход 3,019 с / 71 878 байт. Файл проверен через ffprobe и удалён; модели и воспроизведение не запускались, полный ролик не переводился.
- [x] **361 passed in 9.52s**, синтаксис JavaScript и `git diff --check` прошли; 10 изолированных сценариев UI без ошибок, включая 390 px. Отчёты: `artifacts/verification/source-download-*`.
- [!] Для применения требуется перезапуск UVT и обновление страницы студии. Сервер на обычных портах при диагностике был недоступен; новые рабочие серверы в ходе этой проверки не запускались.

## Проверка

- Python: итоговый совместный запуск — **320 passed in 8.77s** (288 основной набор + 32 файловая панель), `artifacts/verification/pytest.txt`.
- JavaScript: обе рабочие программы проходят `node --check`; userscript проверен в браузере на defaults/overrides, отсутствие моделей, отмену, мобильный экран и fullscreen.
- Интеграция настоящего API: загрузка → этапы → результат → все шесть экспортов, HTML audio декодирует M4A, ffprobe подтверждает видео + две аудиодорожки MKV. Авторизация, подписи ссылок и отмена проверены. Desktop 1280 px и mobile 390 px без горизонтального переполнения, ошибок JS нет. Использовались тестовые движки, а не реальные модели. Отчёт и снимки: `artifacts/workspace-integration/`.
- Независимый обзор downloader/runtime/server/mixing не нашёл подтверждённых регрессий. `git diff --check` проходит.

## Запуск и откат

Из `/Users/teotim/Documents/голос`: `bash scripts/serve-personal.sh`, затем `http://127.0.0.1:8765/workspace`. Обёртка явно выбирает исходники этой папки. Обновить userscript можно через `/workspace/userscript.user.js` или файл `browser/uvt.user.js` в Tampermonkey, после чего нужно перезагрузить вкладку с видео. Текущее состояние пользовательского сервера и применение обновления проверяются отдельно; для нового кода нужен штатный перезапуск.

Изменения не закоммичены. Для отката выбранных файлов сначала сохраните последующие пользовательские правки, затем восстановите требуемые исходники из Git; новые файлы перечислены в `git status`. Ветки, коммиты, публикация и изменения секретов не выполнялись.

## Следующие этапы

- [ ] Потоковая сборка длинных дорожек на диске вместо полной float32 timeline в RAM; затем длинный тест на 60–90 минут.
- [ ] Сравнить длинную речь MOSS с Piper/F5; Qwen3-TTS остаётся следующим кандидатом. Установка и отдельный CPU smoke MOSS уже завершены.
- [ ] Сравнить NLLB 600M и Riva-Translate-4B с текущим TranslateGemma на одном EN/RU/UK корпусе; проверять числа, отрицания, имена и контекст диалога. Повторные короткие задачи всё ещё тратят время на загрузку MT; оценить ограниченный кэш с бюджетом памяти и освобождением при смене профиля.
- [ ] После выполненного английского smoke Nemotron проверить RU/UK и постоянную потоковую сессию; NeMo-Speech.cpp остаётся альтернативным runtime.
- [ ] Chrome extension с tabCapture/AudioWorklet и явным выбором источника; отдельный протокол частичных и окончательных реплик. Для двухстороннего звонка нужны отдельные каналы и защита от захвата собственной озвучки.
