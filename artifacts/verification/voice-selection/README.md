# Проверка голосов и связности речи — 2026-09-06

Итог: **651 тест прошёл за 20,88 с**, синтаксис userscript и `git diff --check` проверены. [Лог](pytest-final.log).

Глобальные пары сохраняются отдельно для движка и языка. В браузере проверены Piper, MOSS, GPT и ElevenLabs: сохранение пары, отдельная роль пробы, переключение профиля и языка. Облачный синтез в UI-проверке заменён нейтральным WAV, квота не расходовалась. Образец F5 загружен через настоящий API (201), его выбор сохранён (200). [UI-протокол](ui-checks.json), [широкий экран](settings-desktop.png), [390 px](settings-mobile.png); горизонтального переполнения нет. Browser plugin отсутствовал, использован доступный Playwright MCP.

MOSS: четыре встроенных голоса реально озвучили русскую фразу на CPU, [WAV и измерения](../moss-voices/results.json). Сохранённые образцы доступны MOSS/F5. Общий экземпляр клонирующей модели сохраняет память, но роль задаётся отдельно на каждую реплику; 7 тестов проверяют чередование, повторы, подгонку длительности и отмену.

[Спикеры](speaker-stability.json): нейтральный голос с двумя вариантами разбивки раньше ошибочно разделялся на двух говорящих, теперь остаётся одним. Пол по высоте тона определяется приблизительно; на пограничной записи стабильная мужская роль является резервным выбором.

[Контекст Hy-MT2](context-hymt-smoke.json): три целевые реплики за 2,495 с после загрузки, соседние строки не попали в результат. Результат на этом примере совпал с переводом без внешнего контекста; модель проигнорировала пожелание использовать «ты». Это доказательство работоспособности передачи контекста, не гарантированного улучшения литературного качества. Полный перенос эмоций исходника не реализован. Новое окружение реплик поддержано Hy-MT2, MLX Chat и OpenAI; NLLB/TranslateGemma сохраняют прежнее поведение. Живой режим использует только уже полученный контекст.

Userscript 0.18.2: 29 сценариев в настоящем Chrome проверяют основной плеер, карточки/ленты/hover-preview, iframe, fullscreen, popup и SPA-переходы. Тестовый Chrome и панель на 57865–57867 закрыты. Пользовательский UVT не перезапускался: изменения применяются после его перезапуска и обновления userscript.
