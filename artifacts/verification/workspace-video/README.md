# Проверка видео-результатов — 2026-09-06

Изменения проверены в `/Users/teotim/Documents/голос`. Пользовательские видео при браузерной проверке не открывались; использован синтетический цветовой тест с двумя звуковыми частотами.

## Автоматические проверки

`PYTHONPATH=src scripts/with-local-runtime.sh .venv/bin/python -m pytest tests/test_server_workspace.py tests/test_workspace_video.py -q --tb=short`

Результат: **47 passed in 3.82s**.

Проверено:

- загрузка видео, точное совпадение байтов оригинала при скачивании;
- MP4 содержит H.264-видео и AAC-звук;
- встроенный видеоресурс отдаёт диапазон байтов с HTTP 206;
- сохранение истории после перезапуска, новые токены доступа вместо старых;
- отсутствие токенов в файле истории, права 0600;
- повторная подготовка готового URL-задания и изменение уровней без повторного получения исходника;
- повторное использование видео, уже полученного при скачивании звука;
- ограничения громкости, размера и очереди;
- отмена дочерней обработки без потери готового перевода;
- ошибка записи истории не удаляет готовый M4A;
- прерванный `audio.play()` не выдаёт ложную ошибку, реальный запрет автовоспроизведения сообщается.

Проверка синтаксиса `workspace.js` также прошла.

## Настоящий браузер

Нейтральный пример: 320×180, 8 секунд; локальный временный сервер на автоматически выделенном порту 60204.

- Браузер декодировал видео: `readyState=4`, длительность 8.011 секунды.
- Плеер показал ссылки «Оригинальное видео» и «Видео с переводом · MP4».
- Оригинал изменён на 0%, перевод на 99% независимо.
- Обе дорожки играли одновременно; измеренная разница текущего времени — 24 мс.
- Пауза остановила обе дорожки; разница текущего времени — 6 мс.
- Кнопка сохранения выбранных уровней повторно подготовила MP4.
- Прямая ссылка `/workspace?job=feedface0101` открыла нужный результат.
- Быстрая последовательность «воспроизведение → пауза» не показала ложной ошибки.
- На ширине 390 px плеер, регуляторы и кнопки помещались без горизонтальной прокрутки.

Временный сервер остановлен (exit 0), вкладка закрыта, ширина браузера восстановлена. Производственный сервер эта проверка не перезапускала.

Эта проверка подтверждает управление и экспорт на нейтральном примере. Она не является измерением качества перевода моделей или просмотром полного пользовательского ролика.
