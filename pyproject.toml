[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "uvt"
version = "0.2.0"
description = "Universal Voice Translator — закадровый AI-перевод любого видео и звука: кнопка в браузере, дубляж файлов, живой режим"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "UVT contributors" }]
keywords = [
    "translation", "voice-over", "dubbing", "speech", "whisper", "tts",
    "vad", "realtime", "ollama", "userscript",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
]

# Ядро специально лёгкое: тяжёлые движки (Whisper, ONNX, TTS) — в extras,
# их модули импортируются лениво, приложение работает и без них.
dependencies = [
    "numpy>=1.24",
    "sounddevice>=0.4.6",
    "soundfile>=0.12.1",
    "soxr>=0.3.7",
    "httpx>=0.27",
    "pydantic>=2.5",
    "PyYAML>=6.0",
    "rich>=13.0",
]

[project.optional-dependencies]
stt = ["faster-whisper>=1.0.0"]
mlx = ["mlx-whisper>=0.4.0; sys_platform == 'darwin'"]
mac-local = [
    "parakeet-mlx==0.5.2; sys_platform == 'darwin'",
    "mlx-whisper>=0.4.0; sys_platform == 'darwin'",
    "mlx-lm==0.31.3; sys_platform == 'darwin'",
    "langid==1.1.6",
    "ctranslate2>=4.8.0",
    "tokenizers>=0.21.0",
    "huggingface-hub>=0.25.0",
    "piper-tts>=1.4.0",
]
vad = ["onnxruntime>=1.17"]
tts = ["edge-tts>=6.1.9"]
piper = ["piper-tts>=1.4.0"]
kokoro = ["kokoro-onnx>=0.4.0"]
# Клонирующая озвучка: голос берётся образцом из самого ролика, реплика
# синтезируется сразу в свой тайминг (без ускорения готового звука).
f5 = ["f5-tts>=1.1.0"]
# Отделение речи от музыки и шумов перед распознаванием и сборкой.
separation = ["demucs>=4.0.0"]
# Полный маршрут «живого» локального дубляжа для Apple Silicon.
natural = [
    "parakeet-mlx==0.5.2; sys_platform == 'darwin'",
    "mlx-lm==0.31.3; sys_platform == 'darwin'",
    "huggingface-hub>=0.25.0",
    "f5-tts>=1.1.0",
    "demucs>=4.0.0",
]
webrtc = ["webrtcvad>=2.0.10"]
server = ["aiohttp>=3.9"]
web = ["yt-dlp[default]>=2026.8.19"]
gui = ["PySide6>=6.6"]
windows = ["PyAudioWPatch>=0.2.12; sys_platform == 'win32'"]
# Рекомендуемый рабочий набор MVP: локальный Whisper + Silero VAD + Edge TTS
recommended = [
    "faster-whisper>=1.0.0",
    "onnxruntime>=1.17",
    "edge-tts>=6.1.9",
]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "aiohttp>=3.9"]

[project.scripts]
uvt = "uvt.cli:main"

[project.urls]
Homepage = "https://github.com/uvt-project/uvt"

[tool.hatch.build.targets.wheel]
packages = ["src/uvt"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
